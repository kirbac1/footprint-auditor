# For developers generating new migrations:
#   uv run alembic revision --autogenerate -m "describe the change"
# The app itself runs migrations through `exposure-auditor migrate` (and
# `serve`, which migrates first); the database URL comes from EA_DATABASE_URL.
[alembic]
script_location = src/exposure_auditor/migrations
prepend_sys_path = src
